import { Shell } from "@/components/shell";
import ChannelSettingsPage from "./page";

export default function ChannelPageRoute() {
  return (
    <Shell>
      <ChannelSettingsPage />
    </Shell>
  );
}
