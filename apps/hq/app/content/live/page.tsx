import { Shell } from "@/components/shell";
import LiveStreamingPage from "@/components/live-streaming-page";

export default function LiveStreamingPageRoute() {
  return (
    <Shell>
      <LiveStreamingPage />
    </Shell>
  );
}
